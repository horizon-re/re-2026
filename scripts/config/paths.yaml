# Core Inputs
raw_requirements_dir: "02_raw_requirements"
hlj_dir: "03_refined_json_normalized"

# Audit & Spine
docs_manifest: "03_enrichment/keyword_extract/docs/manifest.parquet"
sentences_dir: "spine/sentences"
spine_index: "03_enrichment/keyword_extract/aa56cd75-894a-4e89-a2e5-67737b2e17b1/02_spine_index/index.parquet"

# Candidates & Dictionary
candidates_dir: "candidates"
dictionary_dir: "dictionary"

# Graphs & Matrices
graphs_dir: "graphs"
matrices_dir: "matrices"
embeddings_dir: "embeddings/svd"

# Clusters
clusters_global: "clusters/global.assignments.parquet"
clusters_local_dir: "clusters/local"
clusters_descriptors: "clusters/global.descriptors.parquet"

# Knowledge Graph
kg_linking: "kg/linking.parquet"
kg_short_paths: "kg/short_paths.index"
kg_evidence_dir: "kg/evidence"

# Implicit Candidates
implicit_seeds_dir: "implicit/seeds"
implicit_candidates_dir: "implicit/candidates"

# Scoring & Decisions
features_dir: "features"
scores_dir: "scores"
decisions_dir: "decisions"
explanations_dir: "decisions/explanations"

# Mapping & Versioning
mapping_edges: "mapping/edges.parquet"
mapping_deltas_dir: "mapping/deltas"
mapping_ledger: "mapping/ledger.parquet"

# Whitelist
whitelist_scores: "whitelist/scores.parquet"
whitelist_terms_dir: "whitelist"

# HITL
hitl_queue: "hitl/queue.prioritized.parquet"
hitl_batches: "hitl/batches"
hitl_events: "hitl/events"
hitl_propagation_log: "hitl/propagation.log"

# Metrics & Governance
reports_dir: "reports"
governance_snapshots: "governance/weekly_snapshots"
